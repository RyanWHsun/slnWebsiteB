@model IEnumerable<prjWebsiteB.Models.TAttractionImage>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FAttraction)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FImage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FAttraction.FAttractionId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FAttractionImageId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FAttractionImageId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FAttractionImageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
